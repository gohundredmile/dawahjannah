# 🚀 GitHub Releases & In-App OTA Update Guide (দা'ওয়াহ টু জান্নাহ)

## 📌 কীভাবে নতুন APK রিলিজ স্বয়ংক্রিয়ভাবে আপলোড ও প্রতিস্থাপন (Replace) হবে

এই রিপোজিটরিতে **GitHub Actions** অটোমেশন ওয়ার্কফ্লো সংযুক্ত করা হয়েছে:
`.github/workflows/release-apk.yml`

### ১. স্বয়ংক্রিয় বিল্ড ও রিলিজ (Automatic Workflow)
- যখনই আপনি Google AI Studio থেকে গিটহাবে কোড পুশ করবেন (বা `git push` করবেন `main` / `master` ব্রাঞ্চে), সাথে সাথে GitHub Actions স্বয়ংক্রিয়ভাবে রান করবে:
  1. সোর্স কোড থেকে অ্যান্ড্রয়েড APK (`app-release.apk`) বিল্ড করবে।
  2. GitHub Releases-এ `latest` ট্যাগ এবং বর্তমান ভার্সন ট্যাগে (`v1.3.7`) নতুন APK আপলোড করবে।
  3. **স্বয়ংক্রিয় প্রতিস্থাপন (Auto-Replace):** নতুন রিলিজ হলে পুরনো APK স্বয়ংক্রিয়ভাবে নতুন APK ফাইল দ্বারা রিপ্লেস (Replace/Overwrite with `--clobber`) হয়ে যাবে।

### ২. ম্যানুয়াল ট্রিগার (GitHub Actions থেকে এক ক্লিকে রিলিজ)
1. গিটহাব রিপোজিটোরিতে যান: `https://github.com/gohundredmile/dawahjannah`
2. **Actions** ট্যাবে ক্লিক করুন।
3. বাঁ পাশের তালিকা থেকে **"Build & Release APK (OTA Engine)"** সিলেক্ট করুন।
4. **"Run workflow"** বাটনে ক্লিক করুন।
5. ২-৩ মিনিটের মধ্যে নতুন APK বিল্ড হয়ে Releases পেজে চলে আসবে!

### ৩. অ্যাপের ভেতর থেকে ইন-অ্যাপ ওটিএ আপডেট (In-App OTA Update)
- ব্যবহারকারী অ্যাপের **সেটিংস** স্ক্রিন থেকে **"সম্পূর্ণ APK ওটিএ অটো-আপডেট"** বাটনে ট্যাপ করলেই:
  1. গিটহাব থেকে সরাসরি ব্যাকগ্রাউন্ডে নতুন APK ডাউনলোড হবে।
  2. ডাউনলোড শেষ হওয়ামাত্রই ফোনের প্যাকেজ ইনস্টলার ওপেন হবে।
  3. 'Update' দিলেই আগের সব ডেটা বজায় রেখে সম্পূর্ণ অ্যাপ নতুন ভার্সনে আপডেট হয়ে যাবে!

### ৪. ইনস্টল ছাড়াই কনটেন্ট দ্রুত সিঙ্ক (Quick OTA Content Sync)
- যদি ব্যবহারকারী APK ফাইল রি-ইনস্টল করতে না চান, তবে সেটিংসের **"অনলাইন কনটেন্ট দ্রুত সিঙ্ক"** বাটনে চাপ দিলেই গিটহাবের `app-updates.json` থেকে সমস্ত নতুন দো'আ, আমল, ফজিলত এবং নোটিফিকেশন তাৎক্ষণিকভাবে লাইভ সিঙ্ক হয়ে যাবে।

---

## 🛠️ পিসি থেকে সরাসরি ইনস্টল (Install via USB)
যদি ইন্টারনেট ছাড়াই সরাসরি কম্পিউটার থেকে ফোনে ইনস্টল করতে চান:
1. ফোনের **Settings > About Phone**-এ গিয়ে **Build Number**-এ ৭ বার ট্যাপ করে ডেভেলপার অপশন অন করুন।
2. **Settings > System / Developer Options**-এ গিয়ে **USB Debugging** চালু করুন।
3. USB ক্যাবল দিয়ে পিসির সাথে ফোন যুক্ত করে ফোনের স্ক্রিনে **Allow USB Debugging** অনুমতি দিন।
4. Google AI Studio স্ক্রিনের উপরের অংশে থাকা **"Install via USB"** বাটনে ক্লিক করুন।
